import React from 'react';


class Btn extends React.Component {
  render() {
    let styles={
      
    }
    return(
      <button style={styles}>{this.props}</button>
    )
  }
}

export default Btn;
